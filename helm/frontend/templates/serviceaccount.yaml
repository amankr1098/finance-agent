apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "finance-agent-frontend.serviceAccountName" . }}
  labels:
    {{- include "finance-agent-frontend.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false
